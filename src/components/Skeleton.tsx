import { Card } from "@mui/material";
import { CARD_HEIGHT, CARD_WIDTH } from "../constants";

const Skeleton = () => <Card sx={{ height: CARD_HEIGHT, width: CARD_WIDTH }} />;

export default Skeleton;
